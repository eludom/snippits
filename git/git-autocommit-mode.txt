Emacs git-auto-commit-mode: https://github.com/ryuslash/git-auto-commit-mode

This, in fact rocks.  Backups are now irrelevant (at least for emacs
git users).  And you thought VMS file version numbers were pretty neat...


;; -*- eval: (git-auto-commit-mode 1) -*-

foo

bar

baz

qux



; Comment out the auto push because it might be scary
;(setq gac-automatically-push-p t)
(setq gac-ask-for-summary-p t)
(add-hook 'certain-hook 'git-auto-commit-mode)
